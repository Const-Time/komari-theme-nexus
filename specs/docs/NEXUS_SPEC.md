你正在当前仓库中开发一个全新的 Komari 主题。

项目名称：

Komari Nexus

中文名称：

Komari 星枢

GitHub 仓库名称：

komari-theme-nexus

该项目基于：

sanrokamlan-prog/komari-theme-Glassmorphism

但最终交付物必须作为一个独立主题存在。

==================================================
第一部分：项目初始化与品牌迁移
==================================================

开始编码前，请先完整检查：

- git remote
- 当前分支
- package.json
- komari-theme.json
- README.md
- vite.config.ts
- AGENTS.md
- src 下所有相关 AGENTS.md
- 当前主题目录结构

不要直接开始写代码。

第一步：

确认 Git 仓库关系。

目标状态：

origin:

你的账号/komari-theme-nexus

upstream:

sanrokamlan-prog/komari-theme-Glassmorphism

如果 upstream 不存在，请添加：

git remote add upstream https://github.com/sanrokamlan-prog/komari-theme-Glassmorphism

必须保留 upstream。

不要删除原作者仓库来源。

如果当前仓库仍然叫：

komari-theme-Glassmorphism

不要继续使用这个名称作为最终项目名称。

最终项目身份：

仓库：

komari-theme-nexus

主题：

Komari Nexus

中文：

Komari 星枢

==================================================
第二部分：品牌迁移
==================================================

请完成整个项目的品牌迁移。

检查并修改：

- package.json
- komari-theme.json
- README.md
- 页面标题
- Logo 文案
- meta 信息
- 构建输出名称
- ZIP 打包名称
- 所有硬编码主题名称

旧名称：

Glassmorphism

komari-theme-Glassmorphism

替换为：

Komari Nexus

komari-theme-nexus

README 必须更新。

顶部格式：

# Komari Nexus

A next-generation Komari theme based on
komari-theme-Glassmorphism by sanrokamlan-prog.

增加说明：

Enhanced with:

- VPS monitoring dashboard
- Service navigation
- Node service mapping
- LAN/WAN switching
- Responsive desktop/mobile layout

不要删除：

- 原作者署名
- LICENSE
- upstream 来源说明

==================================================
第三部分：主题元数据
==================================================

修改：

komari-theme.json

name:

Komari Nexus

description:

A unified monitoring dashboard combining Komari node monitoring and personal service navigation.

author:

保留原作者信息。

增加：

Komari Nexus customization

version：

重新规划：

0.1.0

不要继续使用原主题版本号。

==================================================
第四部分：开发原则
==================================================

这是对：

sanrokamlan-prog/komari-theme-Glassmorphism

进行二次开发。

必须：

- 保留当前 Vue 技术栈
- 保留 Vite 构建
- 保留现有 Komari API 调用
- 保留现有 store
- 保留历史数据逻辑
- 保留旋转地球组件
- 保留主题切换系统
- 保留现有 UI 体系

禁止：

1. 重写 React

2. 新增独立后端

3. 新增数据库

4. 新增 Bridge 服务

5. 修改 Komari Server

6. 修改 Komari Agent

7. Docker 服务自动发现

8. Docker Socket 访问

9. HTTP 服务健康检查

10. TCP 端口检测

11. 服务在线状态模拟

本项目只实现：

Komari 节点监控

-

个人服务导航

-

内外网访问切换

==================================================
第五部分：设计参考文件
==================================================

项目中提供四张 UI 参考图：

specs/ui/

包含：

desktop-dark.png

desktop-light.png

mobile-dark.png

mobile-light.png

这些图片是最终视觉目标。

注意：

图片中的：

- 节点名称
- IP 地址
- 服务名称
- 数值

全部只是视觉示例。

禁止：

硬编码截图数据。

所有真实数据必须来自：

Komari API

==================================================
第六部分：产品定位
==================================================

Komari Nexus 是一个：

融合 VPS 监控和家庭服务导航的一体化控制台。

目标：

打开一个页面：

可以看到：

1. 所有 VPS 状态

2. 核心性能指标

3. 流量情况

4. 延迟和丢包趋势

5. 每台 VPS 上运行的服务入口

6. NAS 常用服务入口

数据关系：

Komari 节点

|

|---- 服务 A

|---- 服务 B

|---- 服务 C

==================================================
第七部分：不要过度重构
==================================================

不要因为主题改名而大规模移动文件。

优先：

1. 修改项目身份

2. 增加新组件

3. 复用现有逻辑

4. 最小化修改

保持和 upstream 接近。

方便未来同步更新。

只有明确需要时才调整目录结构。

==================================================
第八部分：开发开始前报告
==================================================

在开始正式开发前，请先输出：

1. 当前仓库结构分析

2. 当前主题数据流分析

3. origin / upstream 状态

4. 需要修改的文件列表

5. 实施计划

然后继续开发。

不要只停留在计划阶段。

==================================================
第九部分：首页整体布局规范
==================================================

首页必须围绕：

Komari 监控

-

服务导航

重新组织。

禁止：

- 左侧永久菜单栏
- 底部移动 Tab
- 单独访问模式卡片
- 单独系统运行卡片
- 右侧主机状态卡片
- 右侧网络流量卡片

保留：

原主题旋转地球。

最终首页结构：

顶部区域

↓

旋转地球

↓

全局指标

↓

常用服务

↓

探针面板

页面必须支持：

正常纵向滚动。

不要为了完整显示在一个屏幕内而压缩内容。

==================================================
第十部分：顶部区域
==================================================

顶部显示：

- 站点名称
- 动态欢迎语
- 操作按钮

欢迎语：

根据用户本地时间自动生成。

例如：

上午：

早上好

下午：

下午好

晚上：

晚上好

不要固定写死。

右上角保留：

- 主题切换
- 设置入口
- LAN/WAN 网络模式切换

不要显示：

“当前为内网模式”

这种说明文字。

LAN/WAN：

使用紧凑按钮或者图标。

==================================================
第十一部分：旋转地球
==================================================

必须继续使用当前 Glassmorphism 主题中的旋转地球。

禁止：

重新开发新的地球组件。

桌面端：

位置：

页面右上区域。

作用：

视觉中心。

移动端：

要求：

1. 位于页面最顶部中央

2. 尺寸明显增大

3. 可以被下面指标卡片轻微遮挡

4. 不影响按钮点击

5. 保持性能

浅色和深色：

使用同一个组件。

==================================================
第十二部分：全局指标区域
==================================================

恢复原 Komari 核心指标。

显示：

1.

主机在线数量

2.

CPU 使用率

3.

内存使用率

4.

存储使用率

5.

网络流量

每个指标：

显示：

- 当前值
- 小型趋势图

数据：

必须来自 Komari。

禁止：

模拟数据。

布局要求：

常用服务区域左侧边缘：

必须和指标区域左侧边缘对齐。

探针面板：

也必须保持同一条左边界。

==================================================
第十三部分：服务导航系统
==================================================

新增：

Nexus Service Navigation。

服务配置：

必须独立管理。

推荐结构：

ServiceGroup:

id

name

order

enabled

Service:

id

name

description

icon

groupId

nodeUuid

lanUrl

wanUrl

featured

enabled

order

要求：

同一个服务：

只能配置一次。

通过：

nodeUuid

关联 VPS。

==================================================
第十四部分：服务导航配置管理
==================================================

新增主题设置页面：

Nexus 设置。

包含：

服务分组管理：

支持：

- 创建分组
- 删除分组
- 修改名称
- 排序

服务管理：

支持：

- 新增服务
- 删除服务
- 修改服务
- 设置图标
- 设置名称
- 设置描述
- 绑定节点
- 设置内网地址
- 设置外网地址
- 是否加入常用服务
- 排序

不要把服务写死在 Vue 文件。

==================================================
第十五部分：常用服务区域
==================================================

标题：

常用服务

功能：

支持分组显示。

默认分组：

全部

示例：

媒体

下载

文件

运维

家庭

点击分组：

只展示该分组服务。

服务卡片显示：

- 图标
- 服务名称
- 简短说明

禁止显示：

- 运行中
- 在线
- 离线
- 健康状态

原因：

项目不实现服务健康检测。

==================================================
第十六部分：常用服务布局
==================================================

桌面：

优先展示：

4 个服务卡片。

超过数量：

使用横向 carousel。

支持：

- 鼠标滚轮
- 拖动
- 左右按钮
- 键盘操作

不要：

增加额外跑马灯装饰条。

不要：

重复显示分页条。

移动：

横向滑动。

保持：

触摸体验。

如果服务数量少：

例如：

1 个

2 个

3 个

不要：

创建空白占位。

不要：

让中间出现巨大空洞。

卡片自然靠左排列。

==================================================
第十七部分：内外网访问切换
==================================================

右上角提供：

LAN / WAN 切换。

模式：

auto

lan

wan

配置：

保存：

lanHostPatterns

wanHostPatterns

自动模式：

根据：

window.location.hostname

判断。

规则：

如果匹配内网：

使用 lanUrl

如果匹配公网：

使用 wanUrl

点击服务：

优先：

当前模式对应地址。

如果为空：

自动使用另一个地址。

如果两个地址都为空：

显示不可访问。

保存用户手动选择：

localStorage。

禁止：

通过访问服务判断网络。

禁止：

服务探测。

==================================================
第十八部分：探针面板整体
==================================================

标题：

探针面板

显示：

所有 VPS 节点。

桌面：

每页：

3 台 VPS。

移动：

每页：

2 台 VPS。

支持：

- 左右切换
- 自动轮播
- 固定模式

标题右侧：

增加：

自动轮播开关。

自动轮播：

支持：

- 自定义间隔
- 鼠标悬停暂停
- 用户操作暂停
- reduced motion

==================================================
第十九部分：探针卡片内容
==================================================

每台 VPS 卡片必须显示：

顶部：

- 节点名称
- 国家/地区图标
- 在线状态
- 在线天数
- 价格

不要显示：

IP 地址。

==================================================
第二十部分：探针指标
==================================================

CPU：

显示：

- 当前占用
- 历史曲线

内存：

显示：

- 当前占用
- 已用/总量
- 历史曲线

磁盘：

显示：

- 当前占用
- 已用/总量
- 历史曲线

网络：

显示：

- 上传速度
- 下载速度

流量：

显示：

- 使用百分比
- 使用量
- 限额

网络质量：

延迟：

显示：

- 当前延迟
- 历史趋势

丢包率：

显示：

- 当前丢包
- 历史趋势

所有历史：

必须使用：

Komari真实数据。

禁止：

随机生成图表。

==================================================
第二十一部分：节点服务导航
==================================================

每台 VPS 卡片底部：

显示：

该节点服务。

来源：

Service.nodeUuid

显示：

最多：

4 个服务图标。

超过：

最后一个显示：

三个点图标

-

更多

禁止：

显示：

+2

+3

+4

点击更多：

打开：

Dialog

或者

Sheet

展示该节点全部服务。

服务图标：

只负责入口。

不要显示：

绿色点

红色点

==================================================
第二十二部分：移动端布局
==================================================

移动端：

参考：

mobile-dark.png

mobile-light.png

禁止：

底部导航栏。

禁止：

左侧菜单。

页面：

单列滚动。

==================================================
第二十三部分：移动端顶部
==================================================

地球：

必须：

顶部居中。

尺寸：

大于桌面。

允许：

覆盖部分指标区域。

顶部按钮：

保持：

紧凑。

==================================================
第二十四部分：移动端指标
==================================================

指标：

和桌面保持一致。

不能删除：

CPU

内存

磁盘

网络

流量

允许：

横向滚动。

不要：

缩小到不可阅读。

==================================================
第二十五部分：移动端探针
==================================================

移动端：

每页：

显示两个 VPS。

探针内容：

必须和桌面一致。

包含：

- 节点名称
- 在线状态
- 在线天数
- 价格
- CPU
- 内存
- 磁盘
- 上传
- 下载
- 流量比例
- 延迟历史
- 丢包历史
- 服务入口

不能制作简化版。

==================================================
第二十六部分：视觉设计规范
==================================================

Komari Nexus 保持：

Glassmorphism 风格。

整体方向：

现代

简洁

科技感

家庭服务器控制中心

不要设计成：

企业后台管理系统

传统 Admin Dashboard

过度霓虹赛博朋克

==================================================
深色模式
==================================================

深色主题：

背景：

深海军蓝

黑蓝渐变

卡片：

毛玻璃效果

半透明背景

轻微模糊

柔和边框

颜色：

主要：

蓝色

紫色

辅助：

绿色

橙色

状态：

在线：

柔和绿色

警告：

橙黄色

错误：

红色

禁止：

过度发光

高亮刺眼

大量霓虹边框

==================================================
浅色模式
==================================================

浅色主题：

背景：

白色

浅蓝渐变

卡片：

半透明白色

柔和阴影

边框：

浅灰蓝色

整体：

类似：

现代 iOS / macOS Dashboard

不要：

纯白刺眼

保持：

足够文字对比度。

==================================================
统一设计原则
==================================================

所有组件：

必须共享：

颜色变量

间距变量

圆角变量

阴影变量

不要：

深色写一套 CSS

浅色复制另一套 CSS

应该：

同一个组件

通过主题变量切换。

==================================================
动画规范
==================================================

允许：

- 卡片 hover
- carousel 滑动
- 地球旋转
- 数值变化

要求：

流畅

克制

禁止：

大量弹跳动画

无限循环高耗动画

必须支持：

prefers-reduced-motion

==================================================
可访问性
==================================================

所有交互：

支持键盘。

按钮：

必须有：

aria-label

图标按钮：

必须有：

tooltip

触摸区域：

移动端：

不能过小。

==================================================
第二十七部分：组件拆分规范
==================================================

不要创建巨大 Vue 文件。

按照功能拆分。

建议结构：

src/components/nexus/

包含：

NexusHero.vue

顶部区域

NexusGlobe.vue

地球包装组件

NexusStats.vue

全局指标

NexusServiceGroup.vue

服务分组

NexusServiceCarousel.vue

服务横向展示

NexusServiceCard.vue

服务卡片

NexusProbeCarousel.vue

探针轮播

NexusProbeCard.vue

单个 VPS 卡片

NexusNodeServices.vue

节点服务入口

NexusMoreServices.vue

更多服务弹窗

NexusNetworkSwitch.vue

内外网切换

具体文件名可以按照当前项目规范调整。

==================================================
第二十八部分：状态管理
==================================================

状态分为三类。

第一类：

主题配置状态。

来源：

Komari theme settings

保存：

服务配置

分组

轮播默认设置

网络配置

第二类：

用户本地状态。

保存：

localStorage

例如：

- 手动 LAN/WAN 选择
- 自动轮播开关
- 当前查看页
- 当前服务分组

第三类：

临时 UI 状态。

例如：

- 当前 carousel 页
- 弹窗状态
- hover 状态

不要：

把所有状态塞入一个 store。

==================================================
第二十九部分：服务配置实现
==================================================

服务配置：

必须：

类型安全。

要求：

提供：

默认值

解析函数

异常处理

如果配置不存在：

显示：

空状态

不能：

页面崩溃。

如果 JSON 损坏：

回退：

默认空配置。

如果未来字段变化：

保留：

version

支持迁移。

==================================================
第三十部分：性能要求
==================================================

不要一次加载全部历史数据。

策略：

桌面：

当前显示 3 台节点：

加载历史。

移动：

当前显示 2 台节点：

加载历史。

切换：

再加载下一组。

==================================================
历史数据
==================================================

历史：

必须缓存。

避免：

重复请求。

组件销毁：

清理：

- timer
- websocket
- listener

==================================================
地球性能
==================================================

地球：

只允许初始化一次。

不要：

页面刷新重复创建 WebGL。

切换主题：

只改变样式。

不要重新加载。

==================================================
轮播性能
==================================================

自动轮播：

必须：

组件卸载停止。

页面隐藏：

暂停。

用户操作：

暂时暂停。

支持：

reduced motion。

==================================================
第三十一部分：开发流程
==================================================

按照以下阶段执行。

==================================================
Phase 1
项目分析
==================================================

完成：

- 阅读当前主题
- 分析 API
- 分析组件
- 分析数据流

输出：

架构说明。

==================================================
Phase 2
品牌迁移
==================================================

完成：

- Komari Nexus 名称
- README
- metadata
- package 信息
- 构建名称

确保：

主题身份独立。

==================================================
Phase 3
基础数据层
==================================================

完成：

- Service 类型
- Service Group
- 配置解析
- store
- network mode

==================================================
Phase 4
桌面端
==================================================

完成：

- 顶部区域
- 地球
- 指标
- 常用服务
- 探针面板

优先达到：

desktop-dark.png

desktop-light.png

视觉接近。

==================================================
Phase 5
移动端
==================================================

完成：

- 响应式
- 大地球
- 两个探针分页
- 服务滑动

达到：

mobile-dark.png

mobile-light.png

==================================================
Phase 6
主题设置
==================================================

完成：

Nexus 设置页面。

包括：

服务管理

分组管理

网络设置

轮播设置

==================================================
Phase 7
优化
==================================================

检查：

- 性能
- 动画
- 空状态
- 异常状态
- 长文本
- 小屏幕

==================================================
第三十二部分：测试要求
==================================================

只能使用：

bun

禁止：

npm

pnpm

yarn

必须执行：

bun run lint

bun run build

==================================================
浏览器验证
==================================================

如果环境支持：

使用 Playwright。

验证：

桌面：

1536 × 1024

1440 × 900

1280 × 800

移动：

390 × 844

393 × 852

430 × 932

==================================================
测试场景
==================================================

必须测试：

主题：

- 深色
- 浅色
- 自动主题

服务：

- 无服务
- 一个服务
- 多个服务
- 超多服务

节点：

- 1 个 VPS
- 2 个 VPS
- 3 个 VPS
- 10 个 VPS

状态：

- 在线节点
- 离线节点

地址：

- 只有 LAN URL
- 只有 WAN URL
- 两者都有
- 两者都没有

文本：

- 超长节点名称
- 超长服务名称

动画：

- 开启 reduced motion

==================================================
第三十三部分：视觉验收
==================================================

必须对照：

specs/ui/

四张参考图。

检查：

1.

整体布局

2.

卡片大小

3.

间距

4.

字体层级

5.

地球位置

6.

服务 carousel

7.

探针信息完整度

8.

浅色模式

9.

深色模式

10.

移动端滚动体验

不要：

只确认 build 成功。

必须：

视觉验证。

==================================================
第三十四部分：最终验收标准
==================================================

完成必须满足：

项目：

Komari Nexus

仓库：

komari-theme-nexus

保留：

upstream

完成：

1.

品牌迁移完成。

2.

桌面版完成。

3.

移动版完成。

4.

深色模式完成。

5.

浅色模式完成。

6.

旋转地球保留。

7.

全局指标恢复。

8.

服务导航完成。

9.

服务分组完成。

10.

节点服务绑定完成。

11.

LAN/WAN切换完成。

12.

探针轮播完成。

13.

桌面显示3个探针。

14.

移动显示2个探针。

15.

显示在线天数和价格。

16.

不显示IP。

17.

显示流量百分比。

18.

显示延迟历史。

19.

显示丢包历史。

20.

更多按钮不显示数字。

21.

没有 Docker 自动发现。

22.

没有 HTTP 健康检查。

23.

没有新增后端。

24.

bun run lint 成功。

25.

bun run build 成功。

26.

生成可导入 Komari 的主题 ZIP。

==================================================
第三十五部分：完成报告
==================================================

完成后输出：

1.

修改文件列表。

2.

新增组件列表。

3.

数据结构说明。

4.

服务配置示例。

5.

网络切换逻辑。

6.

桌面/移动布局说明。

7.

测试命令和结果。

8.

构建产物位置。

9.

已知限制。

现在开始执行开发。
